claim:
  name: radiative_rigidity_rank1
  observable: delta_odd
  definition: delta_odd = (1 - R) / (1 + R)
  scope:
    target: proton
    order: O(alpha)
    ir: post_subtracted
  structure:
    k: 1
    factorization: delta_odd(epsilon, Q2) = a(Q2) * b(epsilon)
  decision_rule:
    invariant: I = (d(e1,Q1)*d(e2,Q2))/(d(e1,Q2)*d(e2,Q1)) == 1
    threshold_sigma: 3
